# ===========================================
# ChattyKathys — Environment Variables
# ===========================================
# Copy this file to .env and fill in values:
#   cp .env.example .env
# ===========================================

# Database (PostgreSQL)
# Local Docker:  postgresql://postgres:postgres@localhost:5433/chattykathys
# Fly.io:        automatically set by `fly postgres attach`
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/chattykathys

# Auth — Session Secret
# Generate with: openssl rand -hex 32
# This signs the session cookie. Keep it secret. Change it in production.
SESSION_SECRET=replace-me-with-a-real-secret

# AI Providers
# Get your Anthropic key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-...

# Get your OpenAI key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Which AI provider to use: "anthropic" or "openai"
AI_PROVIDER=anthropic

# App
NODE_ENV=development
PORT=3000
